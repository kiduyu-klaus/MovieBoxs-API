name = "movieboxapi"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Free tier: 100K requests/day, 10ms CPU time (streaming I/O doesn't count)
# The Worker streams video through without buffering → near-zero RAM usage
